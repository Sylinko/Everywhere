<ResourceDictionary
  xmlns="https://github.com/avaloniaui" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
  xmlns:aai="clr-namespace:Everywhere.AI;assembly=Everywhere.Abstractions" xmlns:ac="clr-namespace:Everywhere.Configuration;assembly=Everywhere.Abstractions"
  xmlns:ai="clr-namespace:Everywhere.AI" xmlns:c="clr-namespace:Everywhere.Configuration"
  xmlns:common="clr-namespace:Everywhere.Common" xmlns:interop="clr-namespace:Everywhere.Interop"
  xmlns:md="clr-namespace:LiveMarkdown.Avalonia;assembly=LiveMarkdown.Avalonia" xmlns:mdSkia="clr-namespace:LiveMarkdown.Avalonia;assembly=LiveMarkdown.Avalonia.Svg.Skia"
  xmlns:shadui="clr-namespace:ShadUI.Controls;assembly=ShadUI" xmlns:v="clr-namespace:Everywhere.Views">
  <DataTemplate
    x:Key="{x:Type aai:ModelProviderTemplate}"
    x:DataType="aai:ModelProviderTemplate">
    <StackPanel
      Orientation="Horizontal" Spacing="8">
      <shadui:ThemeToggleBlock>
        <shadui:ThemeToggleBlock.DarkContent>
          <ControlTemplate>
            <Image
              Width="{DynamicResource FontSizeXl}"
              Height="{DynamicResource FontSizeXl}"
              md:AsyncImageLoader.SizeToContent="Manual"
              md:AsyncImageLoader.Source="{Binding DarkIconUrl}"
              mdSkia:SvgImageExtension.Css=":nth-child(0) { font-size: 1px }"/>
          </ControlTemplate>
        </shadui:ThemeToggleBlock.DarkContent>
        <shadui:ThemeToggleBlock.LightContent>
          <ControlTemplate>
            <Image
              Width="{DynamicResource FontSizeXl}"
              Height="{DynamicResource FontSizeXl}"
              md:AsyncImageLoader.SizeToContent="Manual"
              md:AsyncImageLoader.Source="{Binding LightIconUrl}"
              mdSkia:SvgImageExtension.Css=":nth-child(0) { font-size: 1px }"/>
          </ControlTemplate>
        </shadui:ThemeToggleBlock.LightContent>
      </shadui:ThemeToggleBlock>

      <TextBlock
        VerticalAlignment="Center"
        Text="{Binding DisplayNameKey^}"/>
    </StackPanel>
  </DataTemplate>

  <DataTemplate
    x:Key="{x:Type aai:ModelDefinitionTemplate}"
    x:DataType="aai:ModelDefinitionTemplate">
    <TextBlock Text="{Binding Name}"/>
  </DataTemplate>

  <DataTemplate x:Key="{x:Type interop:KeyboardShortcut}">
    <DataTemplate.DataType>
      <x:Type
        x:TypeArguments="interop:KeyboardShortcut" TypeName="ac:SettingsTemplatedItem"/>
    </DataTemplate.DataType>

    <v:KeyboardShortcutInputBox
      MinWidth="160" MinHeight="32"
      HorizontalAlignment="Left"
      Shortcut="{Binding Value, Mode=TwoWay}"/>
  </DataTemplate>

  <DataTemplate x:Key="{x:Type c:CompositeKeyboardShortcut}">
    <DataTemplate.DataType>
      <x:Type
        x:TypeArguments="c:CompositeKeyboardShortcut" TypeName="ac:SettingsTemplatedItem"/>
    </DataTemplate.DataType>

    <v:CompositeKeyboardShortcutInputBox
      MinHeight="32" HorizontalAlignment="Right"
      Shortcut="{Binding Value, Mode=TwoWay}"/>
  </DataTemplate>

  <DataTemplate x:Key="{x:Type common:ColoredIcon}">
    <DataTemplate.DataType>
      <x:Type
        x:TypeArguments="common:ColoredIcon" TypeName="ac:SettingsTemplatedItem"/>
    </DataTemplate.DataType>

    <Button
      HorizontalAlignment="Left" CornerRadius="20">
      <v:IconPresenter
        MinWidth="40" MinHeight="40"
        FontSize="{DynamicResource FontSize2Xl}"
        Icon="{Binding Value}"
        IconSize="{DynamicResource FontSize2Xl}"
        LineHeight="{DynamicResource LineHeight2Xl}"/>

      <Button.Flyout>
        <Flyout>
          <v:IconEditor
            Width="360" Height="500"
            Icon="{Binding Value, Mode=TwoWay}"/>
        </Flyout>
      </Button.Flyout>
    </Button>
  </DataTemplate>

  <DataTemplate x:Key="{x:Type ai:SystemAssistant}">
    <DataTemplate.DataType>
      <x:Type
        x:TypeArguments="ai:SystemAssistant" TypeName="ac:SettingsTemplatedItem"/>
    </DataTemplate.DataType>

    <ToggleSwitch
      Margin="6"
      IsChecked="{Binding ((ai:SystemAssistant)Value).AutoSelect, FallbackValue={x:True}, Mode=TwoWay}"
      OffContent="{I18N {x:Static LocaleKey.SystemAssistant_AutoSelectToggle_Content}}"
      OnContent="{I18N {x:Static LocaleKey.SystemAssistant_AutoSelectToggle_Content}}"/>
  </DataTemplate>

  <DataTemplate
    x:Key="{x:Type c:IWebSearchEngineProvider}"
    x:DataType="c:IWebSearchEngineProvider">
    <DockPanel HorizontalSpacing="6">
      <Image
        DockPanel.Dock="Left"
        Width="{DynamicResource FontSizeXl}"
        Height="{DynamicResource FontSizeXl}"
        VerticalAlignment="Center" md:AsyncImageLoader.SizeToContent="Manual"
        md:AsyncImageLoader.Source="{Binding IconUrl}"
        mdSkia:SvgImageExtension.Css=":nth-child(0) { font-size: 1px }" Stretch="UniformToFill"/>

      <TextBlock
        VerticalAlignment="Center"
        Text="{Binding HeaderKey^}"
        TextTrimming="CharacterEllipsis">
        <Interaction.Behaviors>
          <shadui:TextTrimmedToolTipBehavior/>
        </Interaction.Behaviors>
      </TextBlock>
    </DockPanel>
  </DataTemplate>
</ResourceDictionary>
